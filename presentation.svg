<?xml version="1.0" encoding="UTF-8"?>
<svg id="Layer_1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" viewBox="0 0 1600 900" style="background-color: black;">
<defs>
    <style>
@import url("https://p.typekit.net/p.css?s=1&amp;k=vmw2zhf&amp;ht=tk&amp;f=28161.28163&amp;a=275708143&amp;app=typekit&amp;e=css");

@font-face {
font-family:"bookman-jf-pro";
src:url("https://use.typekit.net/af/05a308/00000000000000007735b3f4/31/l?primer=7cdcb44be4a7db8877ffa5c0007b8dd865b3bbc383831fe2ea177f62257a9191&amp;fvd=i4&amp;v=3") format("woff2"),url("https://use.typekit.net/af/05a308/00000000000000007735b3f4/31/d?primer=7cdcb44be4a7db8877ffa5c0007b8dd865b3bbc383831fe2ea177f62257a9191&amp;fvd=i4&amp;v=3") format("woff"),url("https://use.typekit.net/af/05a308/00000000000000007735b3f4/31/a?primer=7cdcb44be4a7db8877ffa5c0007b8dd865b3bbc383831fe2ea177f62257a9191&amp;fvd=i4&amp;v=3") format("opentype");
font-display:auto;font-style:italic;font-weight:400;font-stretch:normal;
}

@font-face {
font-family:"bookman-jf-pro";
src:url("https://use.typekit.net/af/d24cfd/00000000000000007735b3f6/31/l?primer=7cdcb44be4a7db8877ffa5c0007b8dd865b3bbc383831fe2ea177f62257a9191&amp;fvd=n4&amp;v=3") format("woff2"),url("https://use.typekit.net/af/d24cfd/00000000000000007735b3f6/31/d?primer=7cdcb44be4a7db8877ffa5c0007b8dd865b3bbc383831fe2ea177f62257a9191&amp;fvd=n4&amp;v=3") format("woff"),url("https://use.typekit.net/af/d24cfd/00000000000000007735b3f6/31/a?primer=7cdcb44be4a7db8877ffa5c0007b8dd865b3bbc383831fe2ea177f62257a9191&amp;fvd=n4&amp;v=3") format("opentype");
font-display:auto;font-style:normal;font-weight:400;font-stretch:normal;
}

.tk-bookman-jf-pro { font-family: "bookman-jf-pro",serif; }

.slide, .slide-bg{
    width: 100%;
    height: 100%;
}
.slide{
    display: none;
}

.slide:first-of-type{
    display: block;
}

.slide-bg {
    x:0;
    y:0;
}
text{
    fill: black;
    font-family: "bookman-jf-pro", Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
}

.title{
    font-size: 10vh;
}

.subtext{
    font-size: 6vh;
}


        </style>
</defs>


<g class="slide">
    <rect class="slide-bg"  fill="#d4d6cb" />
    <text class="title" x="10%" y="20%">First</text>
    <text class="subtext" x="10%" y="30%">The quick brown fox jumps over</text>
    <text class="subtext" x="10%" y="40%">the lazy dog.</text>
</g>

<g class="slide">
    <rect class="slide-bg" fill="#d4d6cb"  />
    <text class="title" x="10%" y="20%">Second</text>
    <text class="subtext" x="10%" y="30%">The quick brown fox jumps over</text>
    <text class="subtext" x="10%" y="40%">the lazy dog.</text>
</g>

<g class="slide">
    <rect class="slide-bg" fill="#d4d6cb"  />
    <text class="title" x="10%" y="20%">Third</text>
    <text class="subtext" x="10%" y="30%">The quick brown fox jumps over</text>
    <text class="subtext" x="10%" y="40%">the lazy dog.</text>
</g>

<!-- Fullscreen button -->
<g id="fullscreen-icon" style="cursor: pointer;">
    <rect x="1500" y="20" width="80" height="80" fill="white" opacity="0.3" rx="5"/>
    <text x="1540" y="70" font-size="40" fill="white" text-anchor="middle">⛶</text>
</g>

<script type="text/javascript">
<![CDATA[
    (function() {
        const fullscreenIcon = document.getElementById("fullscreen-icon");
        const svg = document.querySelector('svg');

        if (fullscreenIcon && svg) {
            // Toggle fullscreen on click
            fullscreenIcon.addEventListener("click", function() {
                if (!document.fullscreenElement) {
                    // Enter fullscreen
                    svg.requestFullscreen().catch((err) => {
                        console.error("Error attempting to enable fullscreen:", err);
                    });
                } else {
                    // Exit fullscreen
                    document.exitFullscreen();
                }
            });

            // Show/hide button based on fullscreen state
            document.addEventListener('fullscreenchange', function() {
                if (document.fullscreenElement) {
                    fullscreenIcon.style.visibility = 'hidden';
                } else {
                    fullscreenIcon.style.visibility = 'visible';
                }
            });
        }

        // Helper function to navigate slides
        function navigateSlides(direction) {
            const slides = document.querySelectorAll('.slide');

            // Find currently visible slide by checking computed style
            let currentIndex = -1;
            for (let i = 0; i < slides.length; i++) {
                if (window.getComputedStyle(slides[i]).display === 'block') {
                    currentIndex = i;
                    break;
                }
            }

            let nextIndex = currentIndex;

            if (direction === 'next' && currentIndex < slides.length - 1) {
                nextIndex = currentIndex + 1;
            } else if (direction === 'prev' && currentIndex > 0) {
                nextIndex = currentIndex - 1;
            }

            // Switch slides if index changed
            if (nextIndex !== currentIndex && nextIndex >= 0) {
                slides[currentIndex].style.display = 'none';
                slides[nextIndex].style.display = 'block';
            }
        }

        // Keyboard navigation for slides
        document.addEventListener('keydown', function(e) {
            if (e.key === 'ArrowRight') {
                navigateSlides('next');
            } else if (e.key === 'ArrowLeft') {
                navigateSlides('prev');
            }
        });

        // Touch navigation for mobile
        svg.addEventListener('click', function(e) {
            const svgRect = svg.getBoundingClientRect();
            const clickX = e.clientX - svgRect.left;
            const svgWidth = svgRect.width;
            const leftThird = svgWidth / 3;
            const rightThird = svgWidth * 2 / 3;

            if (clickX < leftThird) {
                navigateSlides('prev');
            } else if (clickX > rightThird) {
                navigateSlides('next');
            }
        });
    })();
]]>
</script>

</svg>
